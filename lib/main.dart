import 'package:flutter/material.dart';

void main () => runApp( const MaterialApp(
  // materialApp is going to act as a wrapper for the rest of our widgets inside
  // the home of our material app is just going to be just a text widget.
  // the home screen is just going to be a text widget.
  home: Text('hey ninjas !'),
//   different properties in our widgets are separated by commas
));  // MaterialApp
